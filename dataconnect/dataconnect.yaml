specVersion: "v1"
serviceId: "invade2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "invade2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
